# Pinned deliberately (AHA-3). `pytest-homeassistant-custom-component` pins an
# exact Home Assistant in turn -- 0.13.357 pins 2026.8.3, which is what the dev
# rig runs -- so leaving it unpinned would let a new HA release change what the
# tests run against without anything in this repo changing. Bump it on purpose,
# and check `dev/README.md`'s rig still matches.
pytest-homeassistant-custom-component==0.13.357
ruff==0.16.5
mypy==2.3.1
